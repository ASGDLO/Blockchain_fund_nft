"use strict";
exports.__esModule = true;
exports.getSignerFromOptions = void 0;
var ethers_1 = require("ethers");
var getSignerFromOptions = function (provider, options, library) {
    var privateKey = options && 'privateKey' in options && options.privateKey;
    var mnemonicPhrase = options && 'mnemonicPhrase' in options && options.mnemonicPhrase;
    var json = options && 'json' in options && options.json;
    var password = options && 'password' in options && options.password;
    var privateKeySigner = privateKey && provider && new ethers_1.ethers.Wallet(privateKey, provider);
    var mnemonicPhraseSigner = mnemonicPhrase && provider && ethers_1.ethers.Wallet.fromMnemonic(mnemonicPhrase).connect(provider);
    var encryptedJsonSigner = json && password && provider && ethers_1.ethers.Wallet.fromEncryptedJsonSync(json, password).connect(provider);
    var optionsSigner = options && 'signer' in options && options.signer;
    return privateKeySigner || mnemonicPhraseSigner || encryptedJsonSigner || optionsSigner || (library === null || library === void 0 ? void 0 : library.getSigner());
};
exports.getSignerFromOptions = getSignerFromOptions;
//# sourceMappingURL=getSignerFromOptions.js.map